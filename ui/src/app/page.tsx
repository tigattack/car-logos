import Search from '@/components/Search';
import React, { use } from 'react';

const HomePage: React.FC = () => {
    return (
        <div>
            <h1>Welcome to Car Logos</h1>
            <Search />
        </div>
    );
};

export default HomePage;
